@page
@model GlobalShopping.Pages.Person.IndexModel

@{
    ViewData["Title"] = "Index";
}

<p class="h1 text-center">Person list</p>

<br />

<p>
    <a class="btn btn-outline-primary" asp-page="Create">Create New</a>
</p>

<div class="container border rounded-3">

    <table class="table table-hover text-center">
        <thead>
            <tr>
                <th scope="col">
                    @Html.DisplayNameFor(model => model.Person[0].FirstName)
                </th>
                <th scope="col">
                    @Html.DisplayNameFor(model => model.Person[0].LastName)
                </th>
                <th scope="col">
                    @Html.DisplayNameFor(model => model.Person[0].DateOfBirth)
                </th>
                <th scope="col"></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.Person)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.FirstName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.LastName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.DateOfBirth)
                    </td>
                    <td>
                        <a class="btn btn-info" asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                        @* <a asp-page="./Details" asp-route-id="@item.ID">Details</a> | *@
                        <a class="btn btn-danger" asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>